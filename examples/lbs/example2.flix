namespace A {
    pub def f(c: Channel[Int]): Unit & Impure = c <- 0; ()
}

def main(_args: Array[String]): Int & Impure =
    let _ = con(jfdujhcvh, chan Int 100);
    let _ = con(Int -> ["bovs"], chan Int 100);
    2 as & Impure

